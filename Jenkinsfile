node {
   
   stage('Code Checkout') { // for display purposes
      // Get some code from a GitHub repository
      git 'https://github.com/pattaabhi/java-apps.git'
     
   }
   stage('Build') {
     
   }
   stage('Unit Test') {
      //junit '**/target/surefire-reports/TEST-*.xml'
      //archive 'target/*.jar'
   }
    stage('Archival') {
      //junit '**/target/surefire-reports/TEST-*.xml'
      //archive 'target/*.jar'
   }
   
    stage('Deploy to Dev') {
      //junit '**/target/surefire-reports/TEST-*.xml'
      //archive 'target/*.jar'
   }
   stage('Smoke Test Execution') {
      //junit '**/target/surefire-reports/TEST-*.xml'
      //archive 'target/*.jar'
   }
}
